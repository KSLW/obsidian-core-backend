var assert = require('assert')
var bitArray = require('../bit-array')
var size = 1000
var b = bitArray(size);
var trues = [0, 4, 31, 998]
trues.forEach(function (i) {
  b.set(i, 1)
})

for (var i = 0; i < size; i++) {
  var set = trues.indexOf(i) !== -1
  assert(!!b.get(i) === set)
}
