var Blockchain = require('cb-blockr')
var coininfo = require('coininfo')
var CoinKey = require('coinkey')
var Wallet = require('./')
var key = CoinKey.createRandom(coininfo('bitcoin-test'))
// var wallet = Wallet.forKey(key.privateWif, new Blockchain('testnet'))
var wallet = Wallet.createRandom({
  network: 'testnet',
  blockchain: new Blockchain('testnet')
})

var coinprops = [
  'privateWif',
  'publicAddress',
  'privateKey',
  'publicKey',
  'publicHash',
  'pubKeyHash',
  'publicPoint',
  'compressed'
]

coinprops.forEach(function(p) {
  console.log(wallet[p])
})

wallet.balance(function(err, balance) {
  console.log(err || balance)
})
